---
import Base from "@base/base.astro";
import { type Lang, supportedLangs } from "@i18n";
import HomeView from "@views/home/index.astro";

export const getStaticPaths = () => {
  return supportedLangs.map((lang) => ({
    params: { lang },
  }));
};

const { lang } = Astro.params as { lang: Lang };
---

<Base lang={lang}>
  <HomeView lang={lang} />
</Base>
