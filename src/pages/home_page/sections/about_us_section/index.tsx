import { SectionWidget } from "@widgets/section_widget";

import { SECTIONS } from "../constants";

export const AboutUsSection = () => {
  return (
    <SectionWidget id={SECTIONS.RESULTS} height="100dvh">
      {SECTIONS.RESULTS}
    </SectionWidget>
  );
};
